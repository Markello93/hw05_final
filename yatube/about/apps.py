from django.apps import AppConfig


class AboutConfig(AppConfig):
    """Класс AboutConfig, определяет
    название приложения about для внесения
    как подкласс в  AppConfig.
    """

    name = 'about'
