from django.apps import AppConfig


class CoreConfig(AppConfig):
    """Класс CoreConfig, определяет
    название приложения core для внесения
    как подкласс в  AppConfig.
    """

    name = 'core'
