from django.apps import AppConfig


class PostsConfig(AppConfig):
    """Класс PostsConfig, определяет
    название приложения posts для внесения
     как подкласс в  AppConfig.
    """

    name = 'posts'
